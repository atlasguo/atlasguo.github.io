#!/bin/sh
set -e

if command -v python3 >/dev/null 2>&1; then
  PY=python3
elif command -v python >/dev/null 2>&1; then
  PY=python
else
  echo "Python is required for pre-commit checks." >&2
  exit 1
fi

$PY scripts/check_mojibake.py --staged
